mod vector;
use vector::Vector;
mod triangle;
use triangle::Triangle;
mod bounding_box;
use bounding_box::BoundingBox;
mod node;
use node::Node;

// fn BVH(vertices: vec<Vector>, triangle_indices: vec<i32>) -> Node {
    
// }

fn main() {
    println!("Hello, world!");
}